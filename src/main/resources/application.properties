spring.config.import=optional:file:.env[.properties]


spring.application.name=user-account-service
server.port=8081


#MYSQL DATABASE CONNECTION
spring.datasource.url=${DB_URL_ACCOUNT}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}


#JPA / Hibernate config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#JWT CONFIG
jwt.secret=${JWT_SECRETE}
jwt.expiration=${JWT_EXPIRATION}

# Actuator Config, Health Monitoring
management.endpoints.web.exposure.include=health,info,metrics

#Eureka Discovery Config
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

#kafka config
spring.kafka.bootstrap-servers=localhost:9092

#Kafka Config as a Producer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.producer.properties.spring.json.add.type=true

#KAFKA CONFIG AS CONSUMER
spring.kafka.consumer.group-id=account-group

# If no committed offset exists, the consumer starts from the beginning of the topic;
# otherwise, it resumes from the last committed offset and processes any missed events.
spring.kafka.consumer.auto-offset-reset=earliest


spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
spring.kafka.consumer.properties.spring.deserializer.value.delegate.class=org.springframework.kafka.support.serializer.JsonDeserializer

spring.kafka.consumer.properties.spring.json.type.mapping=\
  com.example.TransactionsService.kafka.dto.BalanceUpdateEvent:com.example.userAccountService.kafka.dto.BalanceUpdateEvent

eureka.instance.prefer-ip-address=true